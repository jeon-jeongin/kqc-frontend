/**
 * 아이콘 단일 통로 — Tabler Icons 재수출 (강제 규칙 ①의 아이콘 버전)
 * 사용: import { IconSearch, IconEdit } from '@kqc/ui/icons';
 * 아이콘 검색: https://tabler.io/icons
 *
 * 프로덕션 번들에는 실제로 import한 아이콘만 포함된다 (tree-shaking).
 * 단 dev 서버는 전체 카탈로그(~4MB)를 한 번 사전 번들함 — 콜드 스타트가 느려지면
 * 이 파일을 named re-export 목록으로 좁히는 선택지가 있다.
 */
export * from '@tabler/icons-react';
